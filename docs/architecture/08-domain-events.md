# Domain Events

The project supports Domain Events.

However, events are only created when there is a real business consumer.

Events must never exist without a business purpose.

Examples:

- CategoryCreated
- OrderPaid
- CustomerRegistered
