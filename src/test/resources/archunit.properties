
# Modules land one at a time (dev-plan.md): a rule scoped to a technical concept nobody has
# needed yet (e.g. a JDBC Projection) legitimately matches zero classes for a while. Treat that
# as vacuously satisfied instead of a hard failure.
archRule.failOnEmptyShould=false
